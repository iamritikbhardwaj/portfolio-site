# Required for portfolio and ReliefLink form delivery.
# Create this key in Resend and keep the real value server-side.
RESEND_API_KEY=re_replace_with_your_key

# Optional: set to true only when generating a bundle analysis build.
ANALYZE=false
